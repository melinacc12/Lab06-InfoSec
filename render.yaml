services:
  - type: web
    name: okta-oidc-una
    env: node
    buildCommand: npm install
    startCommand: node start.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
